# k6 性能测试模块安装指南

## 功能概述

性能测试模块使用 **k6** 进行性能测试，支持：
- 🤖 **AI 自动生成 k6 脚本**：通过一句话描述，AI 自动生成专业的 k6 性能测试脚本
- ⚡ **自动执行压测**：创建测试后自动执行，实时监控测试状态
- 📊 **智能分析结果**：AI 自动分析性能测试结果，生成专业报告
- 📈 **结构化报告**：易于 AI 解析和分析的 JSON 格式报告

## 安装 k6

### macOS

```bash
# 使用 Homebrew
brew install k6

# 或使用 MacPorts
sudo port install k6
```

### Linux

```bash
# Ubuntu/Debian
sudo gpg -k
sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D9B
echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
sudo apt-get update
sudo apt-get install k6

# CentOS/RHEL
sudo dnf install https://dl.k6.io/rpm/repo.rpm
sudo dnf install k6
```

### Windows

```powershell
# 使用 Chocolatey
choco install k6

# 或使用 Scoop
scoop install k6
```

### Docker

```bash
# 使用 Docker 运行 k6（无需本地安装）
docker run --rm -i grafana/k6 run - <script.js
```

## 验证安装

安装完成后，验证 k6 是否已正确安装：

```bash
k6 version
```

应该看到类似输出：
```
k6 v0.47.0 (go1.21.0, darwin/arm64)
```

## 使用说明

### 1. 创建性能测试

1. 进入 **性能测试** 页面
2. 点击 **创建性能测试** 按钮
3. 填写测试信息：
   - **项目**：选择所属项目
   - **测试名称**：例如 "API 接口压力测试"
   - **测试需求**：一句话描述，例如：
     - "对用户登录接口进行100并发用户持续30秒的压力测试"
     - "测试商品列表接口在50个虚拟用户下的响应时间"
     - "对支付接口进行阶梯式负载测试，从10个用户逐步增加到100个用户"
   - **目标 URL**（可选）：如果测试需求中未明确指定
   - **负载配置**（可选）：虚拟用户数、测试时长等

4. 点击 **创建并执行**
   - AI 会自动生成 k6 脚本
   - 脚本生成成功后，测试会自动开始执行

### 2. 查看测试结果

- 在测试列表中查看测试状态
- 点击 **详情** 查看：
  - **概览**：测试基本信息、性能指标
  - **k6 脚本**：AI 生成的测试脚本
  - **测试结果**：原始测试结果数据
  - **AI 分析**：AI 生成的性能分析报告

### 3. 手动执行和分析

- **执行**：对于已完成的测试，可以点击 **执行** 重新运行
- **分析**：对于已完成的测试，可以点击 **分析** 生成 AI 分析报告

## 功能特点

### AI 自动生成脚本

系统会根据您的测试需求，自动生成符合 k6 最佳实践的测试脚本，包括：
- 合理的负载配置
- 性能检查（checks）
- 阈值设置（thresholds）
- 错误处理
- 思考时间（think time）

### 智能分析报告

AI 分析报告包含：
- **性能评估**：整体性能评级和关键指标
- **响应时间分析**：响应时间分布和趋势
- **吞吐量分析**：系统吞吐量和并发处理能力
- **稳定性分析**：错误率和系统稳定性
- **优化建议**：按优先级排序的性能优化建议
- **风险评估**：性能风险点和容量规划建议

## 注意事项

1. **k6 必须已安装**：如果系统未检测到 k6，性能测试功能将不可用
2. **测试时长**：长时间运行的测试可能会占用较多系统资源
3. **网络环境**：确保测试环境网络连接正常
4. **目标服务**：确保被测试的服务可访问且能够承受测试负载

## 故障排除

### k6 未找到

如果看到 "k6 未安装" 错误：
1. 确认 k6 已正确安装：运行 `k6 version`
2. 确认 k6 在系统 PATH 中：运行 `which k6`（Linux/macOS）或 `where k6`（Windows）
3. 重启后端服务

### 测试执行失败

1. 检查 k6 脚本是否正确生成
2. 检查目标 URL 是否可访问
3. 查看测试详情中的错误信息
4. 检查系统资源是否充足

## 更多信息

- [k6 官方文档](https://k6.io/docs/)
- [k6 最佳实践](https://k6.io/docs/using-k6/best-practices/)
- [k6 脚本示例](https://k6.io/docs/examples/)

